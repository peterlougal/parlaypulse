# ParlayPulse

Live parlay ticket health tracker.

## Setup

1. Clone the repo
2. `npm install`
3. Copy `.env.example` to `.env.local` and add your The Odds API key
4. `npm run dev`

## Deploy on Vercel

Connect the GitHub repo and set the environment variable `ODDS_API_KEY`.
